import { FormContainer } from '../../shared/components'

import { CurrencySwitcher } from './currency-switcher'

export const CurrencySwitchForm = () => {
  return (
    <FormContainer>
      <CurrencySwitcher />
    </FormContainer>
  )
}
